## Task01: Smart Calculator

### Maqsad

Function yozish, parametrlar, return, if/else

### Function’lar

```python
def add(a: float, b: float) -> float
def subtract(a: float, b: float) -> float
def multiply(a: float, b: float) -> float
def divide(a: float, b: float) -> float
def calculate(a: float, b: float, operation: str) -> float
```

---

## Task02: User Input Validator

### Maqsad

String bilan ishlash, boolean return qilish

### Function’lar

```python
def is_valid_username(username: str) -> bool
def is_strong_password(password: str) -> bool
def is_valid_email(email: str) -> bool
```

---

## Task03: ATM Logic

### Maqsad

Real-life mantiq, state bilan ishlash

### Function’lar

```python
def deposit(balance: float, amount: float) -> float
def withdraw(balance: float, amount: float) -> float
def can_withdraw(balance: float, amount: float) -> bool
```

---

## Task04: Text Analyzer

### Maqsad

String manipulation

### Function’lar

```python
def count_characters(text: str) -> int
def count_vowels(text: str) -> int
def is_palindrome(text: str) -> bool
```

---

## Task05: Time & Salary Calculator

### Maqsad

Business logic

### Function’lar

```python
def calculate_salary(hours: int, rate: float) -> float
def calculate_tax(salary: float) -> float
def net_salary(salary: float) -> float
```

---

## Task06: Mini Auth Logic

### Maqsad

Backend thinking

### Function’lar

```python
usernames = 'ali:12345, vali:43144, sami:432, gani:89534'
def authenticate(username: str, password: str) -> bool
def login(username: str, password: str) -> bool
```
